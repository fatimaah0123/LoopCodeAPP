import 'package:flutter/material.dart';

class AppColors {
  // Background colors
  static const bgColor = Color(0xFFFFF8DC); // latar krem pastel
  static const cardBgColor = Color(0xFFFFFBF0); // background card

  // Material colors - lebih vibrant
  static const html = Color(0xFFFFE0B2); // Orange light
  static const css = Color(0xFFBBDEFB); // Blue light
  static const js = Color(0xFFC8E6C9); // Green light

  // Border colors - lebih kontras
  static const htmlBorder = Color(0xFFE65100); // Deep Orange
  static const cssBorder = Color(0xFF1565C0); // Deep Blue
  static const jsBorder = Color(0xFF2E7D32); // Deep Green

  // Gradient colors
  static const htmlGradient = [Color(0xFFFFE0B2), Color(0xFFFFCC80)];
  static const cssGradient = [Color(0xFFBBDEFB), Color(0xFF90CAF9)];
  static const jsGradient = [Color(0xFFC8E6C9), Color(0xFFA5D6A7)];

  // UI colors
  static const primaryPurple = Color(0xFF7B1FA2);
  static const secondaryPurple = Color(0xFF9C27B0);
  static const accentColor = Color(0xFFE91E63);

  // Text colors
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textLight = Color(0xFFBDBDBD);

  // Success, Warning, Error
  static const successColor = Color(0xFF4CAF50);
  static const warningColor = Color(0xFFFF9800);
  static const errorColor = Color(0xFFF44336);

  // Shadow colors
  static const shadowLight = Color(0x1A000000);
  static const shadowMedium = Color(0x33000000);
  static const shadowDark = Color(0x4D000000);
}
